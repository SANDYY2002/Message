export async function migrateCommunity(conn) {
  const statements = [
    `CREATE TABLE IF NOT EXISTS confession_posts(id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,author_id INT UNSIGNED NOT NULL,text TEXT NOT NULL,status VARCHAR(12) NOT NULL,media_path VARCHAR(80),media_mime VARCHAR(80),media_size INT UNSIGNED,created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),INDEX(status,id),FOREIGN KEY(author_id) REFERENCES users(id)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
    `CREATE TABLE IF NOT EXISTS confession_comments(id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,post_id INT UNSIGNED NOT NULL,author_id INT UNSIGNED NOT NULL,text TEXT NOT NULL,anonymous BOOLEAN NOT NULL DEFAULT TRUE,status VARCHAR(12) NOT NULL,created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),INDEX(post_id,id),FOREIGN KEY(post_id) REFERENCES confession_posts(id) ON DELETE CASCADE,FOREIGN KEY(author_id) REFERENCES users(id)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
    `CREATE TABLE IF NOT EXISTS confession_likes(post_id INT UNSIGNED NOT NULL,user_id INT UNSIGNED NOT NULL,PRIMARY KEY(post_id,user_id),FOREIGN KEY(post_id) REFERENCES confession_posts(id) ON DELETE CASCADE,FOREIGN KEY(user_id) REFERENCES users(id)) ENGINE=InnoDB`,
    `CREATE TABLE IF NOT EXISTS confession_reports(id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,post_id INT UNSIGNED NOT NULL,user_id INT UNSIGNED NOT NULL,reason VARCHAR(500) NOT NULL,resolved BOOLEAN NOT NULL DEFAULT FALSE,created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),UNIQUE(post_id,user_id),FOREIGN KEY(post_id) REFERENCES confession_posts(id) ON DELETE CASCADE,FOREIGN KEY(user_id) REFERENCES users(id)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`,
    `CREATE TABLE IF NOT EXISTS superadmins(user_id INT UNSIGNED PRIMARY KEY,secret TEXT NOT NULL,last_step BIGINT NOT NULL DEFAULT -1,FOREIGN KEY(user_id) REFERENCES users(id)) ENGINE=InnoDB`,
    `CREATE TABLE IF NOT EXISTS admin_sessions(token_hash CHAR(64) CHARACTER SET ascii PRIMARY KEY,expires_at DATETIME(3) NOT NULL,FOREIGN KEY(token_hash) REFERENCES sessions(token_hash) ON DELETE CASCADE) ENGINE=InnoDB`,
    `CREATE TABLE IF NOT EXISTS admin_audit(id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,admin_id INT UNSIGNED NOT NULL,action VARCHAR(80) NOT NULL,target_id INT UNSIGNED NULL,created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),INDEX(admin_id,id)) ENGINE=InnoDB`,
    `CREATE TABLE IF NOT EXISTS hidden_chats(user_id INT UNSIGNED NOT NULL,conversation_id INT UNSIGNED NOT NULL,PRIMARY KEY(user_id,conversation_id),FOREIGN KEY(user_id) REFERENCES users(id),FOREIGN KEY(conversation_id) REFERENCES conversations(id)) ENGINE=InnoDB`,
  ];
  for (const sql of statements) await conn.query(sql);
}
